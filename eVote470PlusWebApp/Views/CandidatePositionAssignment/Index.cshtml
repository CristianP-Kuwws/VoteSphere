@model List<CandidatePositionViewModel>
@{
    ViewData["Title"] = "Candidate Position Assignment";
    Layout = "~/Views/Shared/_LeaderLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h4 class="mb-0 fw-semibold">Candidate Position Assignment</h4>
        <small class="text-muted">Assign candidates to electoral positions</small>
    </div>
    @if (!(bool)ViewBag.HasActiveElection)
    {
        <a asp-action="Create" class="btn btn-primary">+ New Assignment</a>
    }
</div>

@if ((bool)ViewBag.HasActiveElection)
{
    <div class="alert alert-warning">
        There is an active election. Assignments cannot be created or deleted.
    </div>
}

<div class="card border-0 shadow-sm">
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Candidate</th>
                        <th>Position</th>
                        <th class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="3" class="text-center text-muted py-4">
                                No assignments registered.
                            </td>
                        </tr>
                    }
                    @foreach (var assignment in Model)
                    {
                        <tr>
                            <td class="align-middle fw-medium">
                                @assignment.CandidateName
                                @if (!assignment.CandidateIsActive)
                                {
                                    <span class="badge bg-warning text-dark ms-1">Inactive</span>
                                }
                            </td>
                            <td class="align-middle">
                                <span class="badge bg-dark">@assignment.PoliticalPositionName</span>
                                @if (!assignment.PositionIsActive)
                                {
                                    <span class="badge bg-warning text-dark ms-1">Inactive</span>
                                }
                            </td>
                            <td class="text-center align-middle">
                                @if (!(bool)ViewBag.HasActiveElection)
                                {
                                    <a asp-action="Delete" asp-route-id="@assignment.CandidatePositionId"
                                       class="btn btn-sm btn-outline-danger">Unlink</a>
                                }
                                else
                                {
                                    <span class="text-muted small">Not available</span>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>